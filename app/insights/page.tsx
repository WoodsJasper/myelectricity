import { AppShell, PageHeader, StatusPill } from '@/components/ui';import { insights } from '@/lib/mock-data';
export default function Insights(){return <AppShell><PageHeader title="Insights" sub="Plain-English observations, not raw inverter codes."/><section className="grid gap-4 px-5 pb-12 md:grid-cols-2 md:px-10">{insights.concat([{id:'best',title:'Your best generation day was 12 June.',body:'That day combined clear solar hours with a useful evening breeze.',tone:'good' as const,confidence:'Based on mock/demo data'},{id:'check',title:'If output stays at zero in good conditions, check the inverter.',body:'We will translate connection problems into simple next steps.',tone:'warning' as const,confidence:'Based on last reading'}]).map(i=><article key={i.id} className="glass rounded-3xl p-6"><StatusPill tone={i.tone==='warning'?'warn':i.tone==='good'?'live':'muted'}>{i.tone==='warning'?'Worth checking':i.tone==='good'?'Good to know':'Insight'}</StatusPill><h2 className="mt-4 text-2xl font-semibold">{i.title}</h2><p className="mt-3 text-slate-300">{i.body}</p><p className="mt-5 text-sm text-slate-500">{i.confidence}</p></article>)}</section></AppShell>}
