import { AppShell, PageHeader, StatusPill } from '@/components/ui';
const steps=[['What do you have?',['Solar panels','Wind turbine','Solar + wind','Battery storage']],['Where is the system?',['Home name','Postcode placeholder']],['How should we connect?',['Inverter account/API','Local network','Modbus','CT clamp/smart meter','Manual for now']],['Tariff details',['Import electricity price, p/kWh','Export price, p/kWh','Standing charge optional']],['System details',['Installed capacity in kW','Solar orientation/tilt optional','Wind turbine rated capacity optional']],['Finish',['Your energy dashboard is ready.']]];
export default function Setup(){return <AppShell><PageHeader title="Set up my system" sub="A simple wizard for solar, wind, or hybrid homes."/><section className="grid gap-4 px-5 pb-12 md:px-10">{steps.map((s,i)=><div key={s[0] as string} className="glass rounded-3xl p-6"><StatusPill tone="muted">Step {i+1}</StatusPill><h2 className="mt-3 text-2xl font-semibold">{s[0]}</h2><div className="mt-4 grid gap-3 md:grid-cols-3">{(s[1] as string[]).map(x=><button key={x} className="rounded-2xl bg-white/10 p-4 text-left text-slate-200 ring-1 ring-white/10 hover:bg-white/15">{x}</button>)}</div></div>)}</section></AppShell>}
